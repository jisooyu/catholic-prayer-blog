import React from 'react';
import CarouselComponent from '../components/CarouselComponent'

const Luminous = ({ prayers, prePrayer }) => {

    return (
        <CarouselComponent prayers={prayers} prePrayer={prePrayer} />
    )
};

export default Luminous;
