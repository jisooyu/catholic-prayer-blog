import React from 'react';
import CarouselComponent from '../components/CarouselComponent'

const Sorrowful = ({ prePrayer, prayers }) => {

    return (
        <CarouselComponent prePrayer={prePrayer} prayers={prayers} />
    )
};

export default Sorrowful;
